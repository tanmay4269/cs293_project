#include "bits/stdc++.h"
#include "utils.cpp"
using namespace std;

void marker_manager(buffer_dict& sell_buffer, buffer_dict& buy_buffer) {
  // match sell and buy
  // remember, its already sorted smartly!
}